import React from 'react';

const FriendsCard = ({friend}) => {
    return (
        <div>
            {
                friend.name
            }
        </div>
    );
};

export default FriendsCard;